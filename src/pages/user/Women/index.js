import React from 'react';

import styles from './Women.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

function Women() {
    return (
        <div>
            <h1>Tai dep trai 123</h1>
        </div>
    );
}

export default Women;
